
forall x :: (x > 0) -> 1 + F(x-1) = F(x);
forall x :: F(0) = 1;
def f(x) {
    assume x >= 0;
    ret := 1;
    if (x = 0) then {
        skip;
    } else {
        ret := 1 + f(x - 1);
    };
    return ret;
    assert RET = F(x);
};

assert x != f(5);
