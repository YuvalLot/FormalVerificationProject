
def add1 (x) {
    assume 1;
    return x + 1;
    assert RET = x + 1;
}

def mul(x, y) {
    assume x > 0 && y > 0;
    if (x = 1) {
        return y;
    } else {
        return mul(x - 1, y);
    };
    assert RET = x * y;
}

